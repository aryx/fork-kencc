To compile kencc you will need to install the 32 bits cygwin environment
(see https://www.cygwin.com/ and setup-x86.exe (not setup-x86_64.exe!)).
You will need to add the gcc-core cygwin packages.

After that, you can follow install.txt
