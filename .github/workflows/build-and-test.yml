# AUTOGENERATED FROM build-and-test.jsonnet DO NOT MODIFY
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
      - run: |
          ./configure
          . ./env
          mk
          mk install
name: build-and-test
on:
  pull_request: null
  push:
    branches:
      - master
  workflow_dispatch: null
